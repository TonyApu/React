import React, { useState } from "react";
import { Table } from "antd";
import moment from "moment";
import ProductModal from "./ProductModal";
const ProductList = ({ products }) => {
  const [isModalOpen, setIsModalOpen] = useState(false);
  const [product, setProduct] = useState({
    id: "",
    name: "",
    price: 0,
    withSauce: false,
    quantity: 0,
    expiredDate: /* @__PURE__ */ new Date(),
    src: ""
  });
  const handleOk = () => {
    setIsModalOpen(false);
  };
  const handleCancel = () => {
    setIsModalOpen(false);
  };
  const columns = [
    {
      title: "T\xEAn m\xF3n",
      dataIndex: "name"
    },
    {
      title: "Gi\xE1",
      dataIndex: "price"
    },
    {
      title: "S\u1ED1 l\u01B0\u1EE3ng",
      dataIndex: "quantity"
    },
    {
      title: "HSD",
      dataIndex: "expiredDate",
      render(text, record) {
        return /* @__PURE__ */ React.createElement("div", null, moment(text).format("DD/MM/YYYY"));
      }
    }
  ];
  return /* @__PURE__ */ React.createElement(React.Fragment, null, /* @__PURE__ */ React.createElement(
    Table,
    {
      rowKey: "id",
      dataSource: products,
      columns,
      onRow: (record) => ({
        onClick: () => {
          setProduct(record);
          setIsModalOpen(true);
        }
      })
    }
  ), /* @__PURE__ */ React.createElement(
    ProductModal,
    {
      products: product,
      isModalOpen,
      handleOk,
      handleCancel
    }
  ));
};
export default ProductList;
