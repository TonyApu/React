import { Redirect } from "umi";
export default (props) => {
  const { isLogin } = false;
  if (isLogin) {
    return /* @__PURE__ */ React.createElement("div", null, props.children);
  } else {
    return /* @__PURE__ */ React.createElement(Redirect, { to: "/login" });
  }
};
